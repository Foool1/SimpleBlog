{% extends 'base.html.twig' %}

{% block title %}Strona główna - SimpleBlog{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4">Witamy na SimpleBlog!</h1>
    <p>To jest przykładowa strona główna Twojego przyszłego bloga.</p>
    <div class="mt-4">
        <a href="{{ path('app_login') }}" class="btn btn-primary me-2">Zaloguj się</a>
        <a href="{{ path('app_register') }}" class="btn btn-secondary">Zarejestruj się</a>
    </div>
</div>
{% endblock %}
